import React from 'react';

export default function ExpenseChart({ data, filterYear }) {
    // lọc dữ liệu từ data ứng với năm (filter year)

    // hiển thị biểu đồ
    return <div className="expense-chart">
        <h3>Đây là biểu đồ, lát nữa cài đặt sau</h3>
    </div>
}