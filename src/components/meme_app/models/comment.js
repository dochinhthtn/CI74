export function addComment(userId, ageId, battleId, content) {

}

export function getAllComments() {
   
}

export function getCommentsByAgeAndBattle(ageId, battleId) {
    // lấy dữ liệu comment từ localstorage => là 1 mảng gồm các comment
    // trả về dữ liệu mà lọc những comment có age_id = "ageId" && battle_id = "battleId"
}